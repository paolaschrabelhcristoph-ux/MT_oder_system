# 数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mt_order?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: wd016034
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  # Thymeleaf模板配置
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

# 服务器端口配置
server:
  port: 8082
